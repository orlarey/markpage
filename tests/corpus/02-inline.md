# Inline formatting

This is **bold**, *italic*, ~~struck through~~, and `code`.

Combined: ***bold italic***, **bold with *italic* inside**.

LaTeX-active characters in prose: 100% & $5 cost #1 in {braces} ~tilde ^caret \backslash _underscore.

A [link to example](https://example.com) and an autolink <https://example.org>.

A code span with active chars: `let x = a & b; y_z = ${cost}`.

Inline newlines fold into
a single paragraph.

A second paragraph, separated by a blank line.
