# Tables

A pipe table:

| Name | Type | Notes |
|---|---|---|
| foo | int | first |
| bar | string | the second one |
| baz | bool | with `inline code` |

A CSV fenced block (rendered to a `tabularx` too):

```csv
Name, Age, City
Alice, 30, Paris
Bob, 25, Lyon
Carol, 42, Marseille
```
